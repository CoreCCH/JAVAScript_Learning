/*
    arithmetic expression is a combination of ...
    operands (values, variable, etc.)
    operators (+ - * / %)
*/

let students = 20;

// students += 1;
// students -= 1;
// students *= 2;
// students /= 2;
students %= 2;

console.log(students);